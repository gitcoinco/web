repos:
- repo: git://github.com/pre-commit/mirrors-isort
  sha: 'v4.3.3'
  hooks:
  - id: isort
    args:
      - --check-only
      - --recursive
      - --settings-path
      - ./setup.cfg
      - .
