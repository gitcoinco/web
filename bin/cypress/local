#! /bin/bash
set -eou pipefail

source bin/cypress/vars.sh

node_modules/.bin/cypress install

echo "Starting without video recording"
node_modules/.bin/cypress run \
  --browser chrome \
  --headed \
  --config video=false